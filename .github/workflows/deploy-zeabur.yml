name: Deploy to Zeabur

on:
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to deploy'
        required: true
        type: choice
        options:
          - backend
          - signal-tracker
          - all
      environment:
        description: 'Environment'
        required: true
        type: choice
        default: 'production'
        options:
          - staging
          - production

env:
  REGISTRY: ghcr.io
  REPO: ${{ github.repository }}

jobs:
  deploy-backend:
    if: github.event.inputs.service == 'backend' || github.event.inputs.service == 'all'
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to Zeabur
        run: |
          npx zeabur@latest auth login --token $ZEABUR_API_KEY
          npx zeabur@latest context set project --name ai-copytrading
          npx zeabur@latest template deploy -f infrastructure/backend.yaml -i=false
        env:
          ZEABUR_API_KEY: ${{ secrets.ZEABUR_API_KEY }}

  deploy-signal-tracker:
    if: github.event.inputs.service == 'signal-tracker' || github.event.inputs.service == 'all'
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to Zeabur
        run: |
          npx zeabur@latest auth login --token $ZEABUR_API_KEY
          npx zeabur@latest context set project --name ai-copytrading
          npx zeabur@latest template deploy -f infrastructure/signal-tracker.yaml -i=false
        env:
          ZEABUR_API_KEY: ${{ secrets.ZEABUR_API_KEY }}
