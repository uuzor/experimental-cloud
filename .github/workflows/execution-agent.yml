name: Build and Push Execution Agent

on:
  push:
    branches: [main]
    paths: [execution-agent/**]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./execution-agent
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/execution-agent:${{ github.sha }}
            ghcr.io/${{ github.repository }}/execution-agent:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
