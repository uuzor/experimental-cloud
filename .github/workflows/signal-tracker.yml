name: Build and Push Signal Tracker

on:
  push:
    branches: [main]
    paths: [signal-tracker/**]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./signal-tracker
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/signal-tracker:${{ github.sha }}
            ghcr.io/${{ github.repository }}/signal-tracker:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
